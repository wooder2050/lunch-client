import React, { Component } from "react";
import "./Header.scss";

class Header extends Component {
  render() {
    return (
      <header className="app-header">
        DAVinCI <strong>LUNCH</strong>
      </header>
    );
  }
}

export default Header;
